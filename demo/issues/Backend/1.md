---
title: "Add rate limiting to API endpoints"
status: "in progress"
system: "Backend"
version: "1.0"
assignee: "api-bot"
priority: "high"
labels:

  - security
  - api
created: "2026-03-15"
number: 1
---

Implement rate limiting on all public API endpoints to prevent abuse.

## Requirements

- [ ] Token bucket algorithm for per-user limits
- [ ] Configurable limits per endpoint
- [x] Return `429 Too Many Requests` with `Retry-After` header
- [x] Redis-backed distributed counter

## Notes

Related to #5 (auth middleware). Should integrate with the existing middleware chain.
